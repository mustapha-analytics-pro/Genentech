# Genentech DCM Pipeline (moved)

The pipeline scripts and full documentation now live in the canonical
client folder inside the main internal repo:

`tcs-chrome-extension/client_cases/im_300072_116___Genentech/dcm_pipeline/`

This repo now only holds the manual QA report HTML for client preview:

- `qa_report.html` — last manual QA report
